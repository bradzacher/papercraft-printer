import * as React from 'react'
import * as ReactDOM from 'react-dom'

import App from '~/App'

// this is the entrypoint for local development
ReactDOM.render(<App />, document.getElementById('root') as HTMLElement)
